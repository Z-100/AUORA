package com.auora.api.components.account.services.mapper;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class AAccountMapperTest {

	@Test
	void toDTO() {
		// Should not need unit test, as project wouldn't compile on error
		assertTrue(true);
	}

	@Test
	void toEntity() {
		// Should not need unit test, as project wouldn't compile on error
		assertTrue(true);
	}
}